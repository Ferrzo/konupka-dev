---
interface Props {
    id: string;
    title: string;
    mainTitle?: boolean;
}

const { id, title, mainTitle } = Astro.props;

---

<section>
    {mainTitle ? <h1 id={id}>{title}</h1> : <h2 id={id}>{title}</h2>}

    <article>
        <slot />
    </article>
</section>