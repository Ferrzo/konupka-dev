---
/* No props or frontend JS is needed for this basic example */
---

<style>
    .container {
      width: 100%;
      height: 100px;
      overflow: hidden;
      background-color: #ccc;
      position: relative;
      border-radius: 8px;
      display: flex;
      align-items: center;
    }
  
    .carousel {
      display: flex;
      width: calc(256px * 5 + 10px * 4); /* Adjust based on the number of boxes and margin */
      animation: moveCarousel 4s linear infinite alternate; /* Adjust the duration here to control speed */
    }
  
    .carousel:hover {
      animation-play-state: paused; /* Pause animation on hover */
    }
  
    .box {
      width: 256px;
      height: 100px;
      background-color: #007bff;
      margin-right: 10px;
      flex-shrink: 0;
    }
  
    @keyframes moveCarousel {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(calc(100% - 1320px)); /* Total width calculated */
      }
    }
  </style>
  
  <div class="container">
    <div class="carousel">
      <div class="box">1</div>
      <div class="box">2</div>
      <div class="box">3</div>
      <div class="box">4</div>
      <div class="box">5</div>
    </div>
  </div>